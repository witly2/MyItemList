const express = require('express');
const listeArticles = require('../controllers/listeController');

const router = express.Router();

//router.get('/listArticles', listeArticles.getListes);

module.exports = router;

